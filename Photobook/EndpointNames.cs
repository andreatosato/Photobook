namespace Photobook;

public static class EndpointNames
{
    public const string GetPhotos = nameof(GetPhotos);
    public const string GetPhoto = nameof(GetPhoto);
    public const string UploadPhoto = nameof(UploadPhoto);
    public const string DeletePhoto = nameof(DeletePhoto);
}
